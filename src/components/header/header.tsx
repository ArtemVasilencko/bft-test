import { CLASSES } from '../../shared/classes';
import './header.css';

function Header() {
  return (
    <header className={CLASSES.HEADER}>
      <h1>BFT-TEST</h1>
    </header>
  );
}

export default Header;
